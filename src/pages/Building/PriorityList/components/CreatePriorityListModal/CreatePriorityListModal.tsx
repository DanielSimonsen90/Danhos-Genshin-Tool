import Modal, { ModalConsumerProps } from "@/components/common/Modal";

export const CreatePriorityListModal = (props: ModalConsumerProps) => (
  <Modal {...props}>
    <h1>Hello, world!</h1>
  </Modal>
);