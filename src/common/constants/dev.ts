export const DEBUGS = {
  // common
  strings: false,

  // pages
  searchQuery: false,

  // components
  searchComponent: false,
  settingsModal: false,
  settingsContainer: true,

  // hooks
  useActionState: false,
  useComponent: false,

  // services
  baseService: false,
  searchService: false,
  imageService: false,

  // stores
  cacheStore: false,
  dataStore: false,
  settingsStore: true,
  regionStore: true,
};