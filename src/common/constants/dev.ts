export const DEBUGS = {
  // common
  strings: false,

  // pages
  searchQuery: true,

  // components
  searchComponent: false,

  // hooks
  useActionState: false,
  useComponent: false,

  // services
  baseService: true,
  searchService: true,
  imageService: false,

  // stores
  cacheStore: false,
  dataStore: false,
};