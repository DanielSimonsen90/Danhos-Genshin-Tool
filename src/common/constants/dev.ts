export const DEBUGS = {
  // common
  strings: false,

  // pages
  searchQuery: false,

  // components
  searchComponent: false,

  // hooks
  useActionState: false,
  useComponent: false,
  useStoreEvent: false,
  useStoreProperty: true,

  // services
  baseService: false,
  searchService: false,
  imageService: false,

  // stores
  cacheStore: true,
  dataStore: false,
};